# Настройка Supabase - Пошаговая инструкция

## Шаг 1: Создание проекта

1. Перейдите на https://supabase.com
2. Нажмите "Start your project"
3. Войдите через GitHub (или создайте аккаунт)
4. Нажмите "New project"
5. Заполните:
   - **Name:** document-explainer (или любое имя)
   - **Database Password:** придумайте надежный пароль (сохраните его!)
   - **Region:** выберите ближайший регион (Europe West для России)
   - **Pricing Plan:** Free (для начала)
6. Нажмите "Create new project"
7. Подождите 2-3 минуты пока проект инициализируется

## Шаг 2: Получение ключей API

1. В левом меню нажмите на иконку ⚙️ (Settings)
2. Выберите "API"
3. Скопируйте:
   - **Project URL** (например: https://xxxxx.supabase.co)
   - **anon public** ключ (длинная строка начинающаяся с eyJ...)

4. Откройте файл `.env.local` в корне проекта
5. Вставьте значения:
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
```

## Шаг 3: Создание таблиц базы данных

1. В левом меню нажмите на иконку 🗄️ (SQL Editor)
2. Нажмите "New query"
3. Откройте файл `supabase/schema.sql` в вашем проекте
4. Скопируйте ВСЁ содержимое файла
5. Вставьте в SQL Editor в Supabase
6. Нажмите "Run" (или Ctrl+Enter)
7. Должно появиться сообщение "Success. No rows returned"

8. Создайте новый query
9. Откройте файл `supabase/functions.sql`
10. Скопируйте и вставьте содержимое
11. Нажмите "Run"

## Шаг 4: Проверка таблиц

1. В левом меню нажмите на иконку 📊 (Table Editor)
2. Вы должны увидеть таблицы:
   - profiles
   - documents
   - analyses
   - chat_messages
   - folders
   - document_folders

## Шаг 5: Создание Storage bucket

1. В левом меню нажмите на иконку 🗂️ (Storage)
2. Нажмите "Create a new bucket"
3. Заполните:
   - **Name:** documents
   - **Public bucket:** ВЫКЛЮЧЕНО (оставьте приватным!)
4. Нажмите "Create bucket"

## Шаг 6: Настройка Storage policies

1. Нажмите на созданный bucket "documents"
2. Перейдите на вкладку "Policies"
3. Нажмите "New policy"
4. Выберите "For full customization"
5. Создайте 3 политики:

### Политика 1: Upload (INSERT)
```sql
CREATE POLICY "Users can upload own documents"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (
  bucket_id = 'documents' AND 
  auth.uid()::text = (storage.foldername(name))[1]
);
```

### Политика 2: Read (SELECT)
```sql
CREATE POLICY "Users can read own documents"
ON storage.objects FOR SELECT
TO authenticated
USING (
  bucket_id = 'documents' AND 
  auth.uid()::text = (storage.foldername(name))[1]
);
```

### Политика 3: Delete (DELETE)
```sql
CREATE POLICY "Users can delete own documents"
ON storage.objects FOR DELETE
TO authenticated
USING (
  bucket_id = 'documents' AND 
  auth.uid()::text = (storage.foldername(name))[1]
);
```

## Шаг 7: Настройка Authentication

1. В левом меню нажмите на иконку 🔐 (Authentication)
2. Перейдите на вкладку "Providers"
3. Убедитесь что "Email" включен (должен быть зеленым)

### Опционально: Google OAuth

4. Найдите "Google" в списке провайдеров
5. Включите его
6. Вам понадобятся:
   - Google Client ID
   - Google Client Secret
   
7. Получить их можно в Google Cloud Console:
   - https://console.cloud.google.com
   - Создайте проект
   - Включите Google+ API
   - Создайте OAuth 2.0 credentials
   - Authorized redirect URIs: `https://your-project.supabase.co/auth/v1/callback`

8. Вставьте Client ID и Secret в Supabase
9. Добавьте их также в `.env.local`:
```env
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret
```

## Шаг 8: Проверка настройки

1. Перезапустите dev сервер:
```bash
npm run dev
```

2. Откройте http://localhost:3000
3. Попробуйте зарегистрироваться
4. Проверьте email (может быть в спаме)
5. Подтвердите email
6. Войдите в систему

## Шаг 9: Тестирование функционала

1. Загрузите тестовый документ (PDF, DOCX или TXT)
2. Проверьте что файл появился в Storage → documents
3. Проверьте что запись создалась в Table Editor → documents
4. Нажмите "Начать анализ"
5. Дождитесь результатов
6. Проверьте что анализ сохранился в Table Editor → analyses
7. Попробуйте экспортировать в PDF

## Troubleshooting

### Ошибка "Invalid API key"
- Проверьте что ключи правильно скопированы в `.env.local`
- Убедитесь что нет лишних пробелов
- Перезапустите dev сервер

### Ошибка "Row Level Security policy violation"
- Убедитесь что выполнили оба SQL скрипта
- Проверьте что пользователь залогинен
- Проверьте что RLS включен для всех таблиц

### Ошибка "Failed to upload file"
- Проверьте что bucket 'documents' создан
- Проверьте что Storage policies настроены
- Проверьте размер файла (<10MB)

### Ошибка "Failed to parse document"
- Проверьте формат файла (PDF, DOCX, TXT)
- Попробуйте другой файл
- Проверьте что файл не поврежден

## Готово! 🎉

Теперь ваше приложение полностью настроено и готово к использованию!

## Следующие шаги

1. Протестируйте все функции
2. Пригласите друзей для тестирования
3. Соберите обратную связь
4. Добавьте новые функции из плана разработки
5. Задеплойте на Vercel

## Полезные ссылки

- [Supabase Dashboard](https://app.supabase.com)
- [Supabase Documentation](https://supabase.com/docs)
- [SQL Editor](https://app.supabase.com/project/_/sql)
- [Storage](https://app.supabase.com/project/_/storage)
- [Authentication](https://app.supabase.com/project/_/auth)
